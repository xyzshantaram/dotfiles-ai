---
order: 130
---

Use the hashline read tool to inspect text files. Each row is a HASH followed by content. The HASH is the anchor for edit calls. Read only rows the tools have not served. A rejected read echoes fresh rows that count as serves. Page large files with offset and limit. Read images with the read_image tool instead.
