# W18 — combined subscription panel (OpenCode GO + Claude/meridian), client plugin.
# The panel lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# the bundle; the host half (./lib) registers the meridian and balance
# same-origin proxy routes.
- insert:
    - id: subscriptions
      name: subscriptions
