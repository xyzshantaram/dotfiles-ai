# User-bubble takeover (client plugin).
# The shadowed user/steering bubbles live in the ./client bundle. The host row
# exists so the client-module registry mounts this package in the web profile
# and serves that bundle in the boot graph.
- insert:
    - id: user-bubble
      name: user-bubble
