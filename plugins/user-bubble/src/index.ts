/**
 * Host row of the user-bubble takeover. It exists only so the
 * client-module registry discovers the `dsh.client` entry; all behaviour
 * lives in the browser bundle.
 */
const name = "user-bubble";
function apply() {}
export { apply, name };
