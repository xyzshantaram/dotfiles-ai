# Quote selection (client plugin).
# The floating quote button and its selection tracking live in the ./client
# bundle. The host row exists so the client-module registry mounts this
# package in the web profile and serves that bundle in the boot graph.
- insert:
    - id: quote-selection
      name: quote-selection
