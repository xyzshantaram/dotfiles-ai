/**
 * Host row of the quote-selection plugin. It exists only so the
 * client-module registry discovers the `dsh.client` entry; all behaviour
 * lives in the browser bundle.
 */
const name = "quote-selection";
function apply() {}
export { apply, name };
