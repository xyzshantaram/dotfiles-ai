/**
 * Host row of the modal plugin. It exists only so the client-module registry
 * discovers the `dsh.client` entry; all behaviour lives in the browser bundle.
 */
const name = "modal";
function apply() {}
export { apply, name };
