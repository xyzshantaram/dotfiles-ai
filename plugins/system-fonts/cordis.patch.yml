# System fonts (client plugin).
# The style injection lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# that bundle in the boot graph.
- insert:
    - id: system-fonts
      name: system-fonts
