/**
 * Host row of the system fonts override. It exists only so the client-module
 * registry discovers the `dsh.client` entry.
 */
const name = "system-fonts";
function apply() {}
export { apply, name };
