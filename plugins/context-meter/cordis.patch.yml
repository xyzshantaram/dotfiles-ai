# Context meter (client plugin).
# The ring lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# that bundle in the boot graph.
- insert:
    - id: context-meter
      name: context-meter
