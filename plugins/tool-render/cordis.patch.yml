# H2+H3 — tool-render (client plugin).
# The row rendering lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# that bundle in the boot graph.
- insert:
    - id: tool-render
      name: tool-render
