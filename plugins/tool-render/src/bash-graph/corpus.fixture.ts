// Test fixture ONLY. Never imported by any bash-graph module (see contract.test.ts).
// Verbatim copy of the prototype CORPUS (everyday set) and GCORPUS (git-commit set)
// array literals from proto/index.html. The module must ship without them (criterion 7).
// Extracted by script, not hand-copied, so the bytes match the prototype exactly.

export const CORPUS = ["cd /home/sid/ai-scratch/split-utils && sed -n '48,80p' PLAN.md", "cd /home/sid/ai-scratch/split-utils && rg -n 'escapeHtml' wizardkit/toolkit.tsx | head", "cd /home/sid/ai-scratch/split-utils && rg -n '^- \\[ \\] (G|K)[0-9]+' PLAN.md | head -14", "cd /home/sid/ai-scratch/split-utils && rg -n '^- \\[ \\] K|^Closed in the toolkit' PLAN.md", "cd /home/sid/ai-scratch/split-utils && rg -c '^- \\[ \\]' PLAN.md && rg -n '^- \\[ \\] (G|K)[0-9]+' PLAN.md", "cd /home/sid/ai-scratch/split-utils && rg -c '^- \\[ \\]' PLAN.md && rg -n '^- \\[ \\] (G|K)' PLAN.md | head -20", "cd /home/sid/ai-scratch/split-utils && rg -c '^- \\[ \\]' PLAN.md && rg -n 'cliffy|Cliffy|exotui|MepCLI|mepcli' PLAN.md", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; deno check --no-lock wizards/expense-split.ts 2>&1 | tail -2", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; deno check --no-lock src/reset.ts 2>&1 | tail -2; sed -n '28,62p' src/reset.ts", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; deno task check 2>&1 | tail -2; deno task test 2>&1 | rg -N 'passed|FAILED' | tail -2", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; deno task check 2>&1 | tail -1; deno task test 2>&1 | rg -N 'passed|FAILED' | tail -2", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; rg -n 'answers\\(' wizards/expense-split/gather.ts && deno check --no-lock wizards/expense-split/gather.ts 2>&1 | tail -2", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache; deno check --no-lock wizards/expense-split/settings.ts 2>&1 | tail -3; sed -n '42,60p' wizards/expense-split/settings.ts", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\necho \"=== guard count for the tick error ===\"; rg -c 'Tick at least one order' wizards/expense-split.ts\necho \"=== missing-run path now errors ===\"; rg -n 'The gathered run is gone' wizards/expense-split.ts\ndeno task check 2>&1 | tail -1; deno task test 2>&1 | rg -N 'passed|FAILED' | tail -1; deno lint 2>&1 | tail -1", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\necho \"=== done action left in the app ===\"; rg -n 'action: \"done\"' wizards/expense-split*.ts wizards/expense-split/*.ts || echo \"none\"\necho \"=== hub uses of manualPicked ===\"; rg -c 'manualPicked' wizards/expense-split.ts || echo \"none left in the hub\"\ndeno task check 2>&1 | tail -2; deno task test 2>&1 | rg -N 'passed|FAILED' | tail -2; deno lint 2>&1 | tail -2", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\necho \"=== piped run now ===\"; timeout 40 deno run --no-lock -A wizards/gatherer.ts --login=nosuch 2>&1 | head -3\necho \"=== env-file machinery in the terminal kit ===\"\nrg -n 'ENV_FILE|WRITTEN_ENV|WRITTEN_SECRET|SKIPPED|writeEnv|saveEnv|envAnswer' src/wizardkit.ts | head -20\necho \"=== who calls those exports ===\"\nrg -n 'writeEnv|saveEnv|envAnswer|ENV_FILE' --glob '!state/**' --glob '!src/wizardkit.ts' | head -12", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\npython3 - <<'PY'\nimport pathlib, re\np = pathlib.Path(\"tests/gatherer-login.test.ts\")\nt = p.read_text()\nt2 = re.sub(r'\ns*SPLIT_UTILS_EMBEDDED: \"1\",', '', t)\np.write_text(t2)\nprint(\"test env flag dropped:\", t != t2)\nPY\necho \"=== state carried in the environment, outside the retired wizards ===\"\nrg -n 'SPLIT_UTILS_EMBEDDED|SPLIT_UTILS_FROM_MENU' --glob '!state/**' --glob '!docs/**' | rg -v '^wizards/(pusher|meta|splitter-cliffy|splitter-kit).ts:' || echo \"none\"\ndeno check --no-lock src/wizardkit.ts wizards/gatherer.ts 2>&1 | tail -1\ndeno test --no-lock -A --no-check tests/gatherer-login.test.ts 2>&1 | rg -N 'passed|FAILED'", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\necho \"=== unused import gone? ===\"; rg -c 'manualPicked' wizards/expense-split.ts || echo \"none in the hub\"\ncat >> tests/expense-split-gather.test.ts <<'TS'\n\n// The manual screen states its own condition now. A jump from the\n// accounts step used to skip it, which kept the rule in the submit hook\n// rather than on the step it governs.\nDeno.test(\"the manual screen applies only when Manual is picked\", () => {\n  const built = gatherSteps().map((entry) =>\n    typeof entry === \"function\" ? entry(new Map([[\"platforms\", [\"Manual\"]]])) : entry\n  );\n  const manual = built.find((s) => (s as { id: string }).id === \"gather-manual\") as\n    | { when?: (m: Map<string, string[]>) => boolean }\n    | undefined;\n  if (manual === undefined) throw new Error(\"no gather-manual step\");\n  if (manual.when === undefined) throw new Error(\"gather-manual carries no condition\");\n  if (manual.when(new Map([[\"platforms\", [\"Manual\"]]])) !== true) {\n    throw new Error(\"the screen must apply when Manual is picked\");\n  }\n  if (manual.when(new Map([[\"platforms\", [\"Zepto\"]]])) !== false) {\n    throw new Error(\"the screen must not apply without Manual\");\n  }\n});\nTS\ndeno task check 2>&1 | tail -1; deno task test 2>&1 | rg -N 'manual screen applies|passed|FAILED' | tail -3; deno lint 2>&1 | tail -1", "cd /home/sid/ai-scratch/split-utils && export DENO_DIR=/tmp/dsh/deno NPM_CONFIG_CACHE=/tmp/dsh/npm-cache\ncat >> tests/reset.test.ts <<'TS'\n\n// A reset must clear the old cache copies too. The move forward copies\n// whichever one survives back into the state root on the next read, so\n// a reset that spares them undoes itself and the user stays signed in.\nDeno.test(\"factory reset clears the legacy credential files\", async () => {\n  const base = await Deno.makeTempDir({ dir: \"/tmp\", prefix: \"reset-legacy-\" });\n  const home = base + \"/home\";\n  const root = base + \"/state\";\n  await Deno.mkdir(home + \"/.cache/ordersplit\", { recursive: true });\n  await Deno.mkdir(root + \"/config\", { recursive: true });\n  const oldToken = home + \"/.cache/ordersplit/splitwise_token.json\";\n  await Deno.writeTextFile(oldToken, '{\"oauth_token\":\"t\",\"oauth_token_secret\":\"s\"}');\n  const priorHome = Deno.env.get(\"HOME\");\n  Deno.env.set(\"HOME\", home);\n  Deno.env.set(\"SPLIT_UTILS_STATE\", root);\n  try {\n    const { factoryReset } = await import(\"../src/reset.ts\");\n    const removed = await factoryReset();\n    if (!removed.includes(oldToken)) {\n      throw new Error(\"the removed list does not name the legacy token file\");\n    }\n    let stillThere = true;\n    try {\n      await Deno.stat(oldToken);\n    } catch {\n      stillThere = false;\n    }\n    if (stillThere) throw new Error(\"the legacy token file survived the reset\");\n    // A second run must stay quiet rather than throw on the missing file.\n    await factoryReset();\n  } finally {\n    if (priorHome !== undefined) Deno.env.set(\"HOME\", priorHome);\n    await Deno.remove(base, { recursive: true }).catch(() => {});\n  }\n});\nTS\ndeno test --no-lock -A --no-check tests/reset.test.ts 2>&1 | rg -N 'legacy|passed|FAILED'","cd ~/.dsh/aidos/storage/--home-sid-repos-aidos-- && T=$(mktemp -d); cp board.db \"$T/a.db\"; echo \"=== aidos: every marker's claimed import counts ===\"; sqlite3 \"$T/a.db\" \"select payload from events where kind='backfill/completed' order by seq;\" | python3 -c \"\nimport sys,json\ntot_t=tot_e=0\nfor line in sys.stdin:\n    line=line.strip()\n    if not line: continue\n    p=json.loads(line)\n    tot_t+=p.get('tickets',0); tot_e+=p.get('evidence',0)\n    print('  v%s/imp%s sessions=%-4d tickets=%-4d evidence=%-4d' % (p.get('version'),p.get('importerVersion'),len(p.get('sessionIds',[])),p.get('tickets',0),p.get('evidence',0)))\nprint('TOTAL claimed imported: tickets', tot_t, 'evidence', tot_e)\n\"; rm -rf \"$T\"; echo; cd ~/.dsh/aidos/storage/--home-sid-repos-dotfiles-ai-- && T=$(mktemp -d); cp board.db \"$T/d.db\"; [ -f board.db-wal ] && cp board.db-wal \"$T/d.db-wal\"; [ -f board.db-shm ] && cp board.db-shm \"$T/d.db-shm\"; echo \"=== dotfiles-ai store ===\"; sqlite3 \"$T/d.db\" \"select 'tickets', count(*) from tickets; select 'events', count(*) from events; select 'markers', count(*) from events where kind='backfill/completed';\"; sqlite3 \"$T/d.db\" \"select payload from events where kind='backfill/completed' order by seq desc limit 1;\" | python3 -c \"\nimport sys,json\np=json.loads(sys.stdin.read().strip()); print('latest: v%s/imp%s sessions=%d tickets=%d evidence=%d' % (p.get('version'),p.get('importerVersion'),len(p.get('sessionIds',[])),p.get('tickets',0),p.get('evidence',0)))\n\"; rm -rf \"$T\""];

export const GCORPUS=["cd /home/sid/repos/dotfiles-ai && git add plugins/bash-guard.test.ts && git commit -q -m 'test(bash-guard): the live short-circuit test now catches the misrender (#144 review)' -m 'The arity backstop was pinned only at render level: the misrender contains \"[exit codes (\" rather than \"[exit codes:\" and still prints the scope note, so it slipped past both existing assertions on the live path. Verified: dropping the conjunct now reddens 2 tests (live + render) instead of 1.' && git log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/shared/shims.d.ts && git commit -q -m \"docs(shims): the react shim comment described the pre-#158 alias\n\nThe comment told every future reader to write \\`import * as react\\` and call\n\\`react.useState\\`, which is exactly the convention #158 removed because\nrules-of-hooks cannot see it. It now names the capital-R form and records\nWHY it is load-bearing rather than stylistic, plus the two other places\n(tsconfig.json jsxFactory, build.mjs) that must agree or the bundles\nreference a binding that does not exist.\n\nStale guidance in a file whose whole job is to tell you how to import is\nworse than none.\" && git log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/tool-render/src/bash-sequence.test.ts && git commit -q -m 'test(tool-render): pin the heredoc same-line guard, which was reachable after all (#160 review)' -m 'Reported as unreachable defense-in-depth; the review found the input that reaches it. With the guard removed, `cat <<A/bodyA/A/cat <<B/A/B` DRAWS, reconstructs byte-for-byte and verifies true, while both bodies come out empty -- a misquote that passes every fidelity check. The pre-existing case stayed null in both variants via an unrelated downstream null, which is why the suite stayed green. Mutation: 1 red (this pin), 47 green; restored byte-identical.' && git log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/tool-render/src/client.module.css plugins/tool-render/src/run-code-layout.test.ts plugins/tool-render/dist/client.js && git commit -q -m 'fix(tool-render): the layout selector must reach through the renderer slot wrapper (#152)' -m 'Measured in the live DOM instead of inferred: the renderer wraps every slot entry in <div data-slot=\"tool.call.toolview\" style=\"display:contents\">, so our card is a GRANDchild of the call row and the previous :has(> .tool-render-card) matched nothing -- ruleLoaded true, selectorMatches false, no error, no visual clue. The wrapper generates no box, so our two halves are still flex items of the call row; only the selector was wrong. Pinned so a future edit cannot drop the wrapper step.' && git log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nchore(sync): bump the compaction pin to c349aab (#153)\n\n9525c72 -> c349aab (xyzshantaram/dsh-compaction-instant, branch\nfix/retention-and-shrink-gate). Also picks up be07b43 and 0e304e0: the pin\nhad drifted two commits behind the fork's head.\n\nc349aab stops compiling a subagent's report as ordinary user text. A\nbackground subagent's result reaches the orchestrator as a USER-role\nmessage, so in a checkpoint it read exactly like something the human typed,\nand like the orchestrator's own narration. The checkpoint is re-read by the\nmodel, so that promoted a worker's claim into the agent's own conclusion.\nReports now carry a [subagent] header and a fenced body naming the author;\nthe fence is sized from the content because reports contain their own\nfences. 8 tests upstream.\n\nbash -n clean. Not active until sync.sh runs.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/composer-approvals/src/hooks.test.ts && git commit -q -F - <<'COMMITMSG'\ntest(composer-approvals): the hook pin now sees CUSTOM hooks too (#150)\n\nThe reviewer of 5e64d9a accepted the pin and named the one real hole left\nin it: the scan matched `react.useX` only, so a CUSTOM hook moved below\nthe badge gate -- useBuiltInSurfaces, or props.useSession -- would break\nhook order just as fatally and be completely invisible to the test. That\nis not a hypothetical: useBuiltInSurfaces sits four lines above the gate\ntoday, with a comment explaining that it must, and the original pattern\ncould not have enforced that comment either.\n\nWidened to any `useX(` call, and comment lines are now skipped so prose\nnaming a hook cannot trip the pin -- this file leans hard on its comments\nand a test that punished them would be a bad trade.\n\nMutation B, the case the old pattern missed: move the useBuiltInSurfaces\ncall below the gate -> 1 failed | 2 passed, offender reported as\n\"958: var surfacesBox = useBuiltInSurfaces(\". Restored byte-identical\n(git diff empty against 5e64d9a), 3 green again. Mutation A (the original\nreact.useRef move) still turns 2 of 3 red.\n\ntsc clean; full suite unchanged.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nchore(sync): bump the compaction pin so subagent reports are attributed (#153)\n\n9525c72 -> c349aab (xyzshantaram/dsh-compaction-instant, branch\nfix/retention-and-shrink-gate). Picks up be07b43 and 0e304e0 as well: the\npin had drifted two commits behind the fork's head.\n\nThe new commit stops compiling a subagent's report as ordinary user text.\nA background subagent's result reaches the orchestrator as a USER-role\nmessage, so in a checkpoint it read exactly like something the human typed\n-- and like the orchestrator's own narration. Since the checkpoint is\nre-read by the MODEL after compaction, that quietly promoted a worker's\nCLAIM into the agent's own CONCLUSION. This repo has paid for that\nconfusion more than once (#81, #106, #125 all turned on \"did the agent\nverify this, or repeat it?\").\n\nReports now get a [subagent] role header and a fenced body whose info\nstring names the author, so attribution is machine-readable rather than a\nvisual cue. The fence is sized FROM the content -- one backtick longer than\nthe longest run inside -- because reports routinely contain their own code\nfences, and a fixed three-backtick wrapper would terminate at the report's\nfirst inner fence and spill the rest into the document. The body stays\nbyte-exact. Anything unrecognised stays user text: mislabelling the human\nas a subagent is worse than missing a report.\n\n8 tests upstream, covering both delivery shapes, the negative cases, and\nfence growth past three- and four-backtick runs. Verified in a scratch\nclone before pushing. That repo's suite has a pre-existing unrelated\nfailure, confirmed by stashing the change and reproducing it on a clean\ntree.\n\nbash -n clean. Not active until sync.sh runs.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/quote-selection && git commit -q -F - <<'COMMITMSG'\nfix(quote-selection): table cells are block boundaries, and fence on either endpoint (#155 review)\n\nThree findings from the review of fc4fc04, two of them real defects.\n\n1. TABLE CELLS CONCATENATED. TR was in the block-tag map but TD and TH were\nnot, so a selection spanning two cells of ONE row had no boundary between\nthem and produced \"firstsecond\". Assistant messages render tables\nconstantly, so this was malformed output from ordinary content, not an\nexotic edge. DL/DT/DD are the same shape and go in with them.\n\n2. FENCING CHECKED THE ANCHOR ONLY. A selection starting inside a <pre> and\nending outside it fenced prose; the reverse emitted code as prose. Now\neither endpoint being inside a <pre> fences. Same-row gating bounds how far\nsuch a selection can stretch but does not prevent it.\n\n3. A COMMENT OVERSTATED ITS EVIDENCE. It claimed data-chat-flow-kind \"is\nread by the ship's own scroll and paging code, so it is a load-bearing\ncontract\". The reviewer checked: the attribute has exactly ONE occurrence in\nthe shipped bundle, the write. Scroll and paging key on\ndata-chat-anchor-key and data-conversation-scroll. It remains the best key\navailable -- ship-stamped and stable, not a hashed class -- but it is a\npublic hook, not a contract the ship would notice breaking, and the comment\nnow says so. A justification that overstates its evidence is worse than a\nthin one, because the next reader trusts it.\n\nThe append-contract test now states its own scope honestly too: it pins the\ntwo load-bearing lines (trim the tail, join with a blank line), so drift\nTHERE fails; drift elsewhere in the helper passes silently. It was described\nas catching any drift, which it does not.\n\nNew pins are source-text, not behavioural -- the map lives in client.tsx and\nneeds a DOM, and this repo has no jsdom harness. They discriminate removal of\nthese entries, which is exactly how the defect got in.\n\ntsc clean; full suite 62 files / 1086 passed / 1 skipped (1084 before, +2).\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add eslint.config.mjs package.json pnpm-lock.yaml plugins/composer-approvals/src/client.tsx && git commit -q -F - <<'COMMITMSG'\nchore(lint): a minimal ESLint setup, and the honest news that it is blind here (#154)\n\nThis repo had NO linter, yet plugins/composer-approvals/src/client.tsx\ncarried an `// eslint-disable-next-line react-hooks/exhaustive-deps` --\na suppression for a rule nothing ran, implying coverage that did not exist.\n#150 then shipped a `react.useRef` below an early return, crashed a slot\nentry with React #310, and silently disabled the composer attention badge.\n\nThe ticket's premise was that rules-of-hooks would have caught that for\nfree. THE WORKER TESTED THAT PREMISE AND IT IS FALSE. eslint-plugin-react-hooks\nv7 recognises hooks only through a namespace object literally named `React`\n(hardcoded in getNodeWithoutReactNamespace / isReactFunction and in\n`callee.object.name === 'React'` checks). This repo uses `import * as react`\neverywhere, so the rule sees nothing. Probes:\n\n  bare useRef + early return    -> error, exit 1\n  React.useRef + early return   -> error, exit 1\n  react.useRef + early return   -> SILENT, exit 0\n  the exact #150 mutation       -> SILENT, exit 0\n\nSo this commit adds a linter that is real but currently blind, and says so\nin the config header rather than implying protection it does not give.\n#158 tracks the `react` -> `React` codemod that makes it fire, with the\nbefore/after mutation transcript as its criterion.\n\nConfig: flat ESM, exactly one rule (rules-of-hooks: error), @babel/eslint-parser\nbecause @typescript-eslint/parser hard-refuses TS 7. Globs plugins/**/*.ts(x);\nignores node_modules, lib/, dist/, generated .js/.mjs/.cjs bundles and *.d.ts.\nScript: `pnpm lint`.\n\nNOT WIRED AS A GATE, deliberately: `npx eslint plugins` currently reports 5\npre-existing findings in plugins/tool-render/src/client.tsx -- one false\npositive (a plain function PARAMETER named useSession colliding with the\nuse* heuristic) and four instances of the deliberate\n`typeof useX === \"function\" ? useX(...) : undefined` capability probe. A\npermanently red gate gets ignored, which is the exact failure this ticket is\nabout. It becomes blocking after #158 and those five are resolved.\n\nThe stale disable-comment is replaced by a note that exhaustive-deps is\ndeliberately not enabled.\n\ntsc exit 0 before and after the install; suite unchanged; every plugin\ndependency still resolves (checked explicitly -- an earlier root install\ntoday pruned composer-menu's @radix-ui entry, so this was verified, not\nassumed).\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nfix(sync): preserve user-written settings sections across the regeneration (#159)\n\nThe subscriptions panel forgot which providers were visible. Set\nopencode-go on and zen off; after a sync it was back. The cause was not in\nthat plugin: sync.sh regenerates $DSH_HOME/settings.yaml from the repo\ntemplate, and the plugin persists visibility by writing a `subscriptions`\nnamespace INTO that file. The copy destroyed it.\n\nTHE POINT IS THAT THIS WAS NEVER SUBSCRIPTIONS-SPECIFIC. sync.sh preserved\nexactly two pieces of runtime state across the regeneration -- profile.active\nvia sed and profile.chains via preserve-chains.mjs -- and every other\nnamespace any plugin wrote there was destroyed, silently, forever. Each new\nplugin inherited that.\n\nEnumerated rather than guessed (rg over installSettingsSection /\nsettingsNamespace / settings replace across plugins/): only `profile` and\n`subscriptions` are runtime-written by repo plugins today. `prices`,\n`log-viewer` and the upstream namespaces have no write path and stay\ntemplate-owned.\n\nSo the fix is GENERIC, not a third special case: preserve-user-sections.mjs\ncarries over any top-level section present in the previous file and absent\nfrom the fresh template. subscriptions is covered today and the next\nplugin's namespace is covered with no new code -- which matters, because\nadding a third hand-rolled preservation beside active and chains would have\nbeen another instance of the pattern that caused this.\n\nBoundaries, all documented in the script header: the template stays\nauthoritative for sections it owns (copy wins), `profile` is skipped\nbecause the existing preservations own it, and `modelSync` is skipped\nbecause the M18 strip owns it. Pure-text splice with no YAML parsing, so the\nYAML 1.1 `off:` -> `false:` rewrite hazard the old comments warn about\ncannot occur by construction.\n\nTest drives the REAL extracted step_set_defaults, not a reimplementation.\nI verified the discrimination myself rather than taking it on report:\ndisabling the preservation call turns 4 of 7 red (subscriptions survival,\na never-before-seen future namespace, bare-`off:` byte-identity, and\nsecond-sync stability), while chains/active, template-authority and the\nmodelSync strip correctly stay green. Restored, 7/7.\n\nThe worker additionally verified real behaviour against a lived-in copy of\nthe full 1834-line template: subscriptions preserved with exact values,\nactive: work kept, custom chains rung kept, bare `off:` count 19 = the\ntemplate's 19, modelSync absent.\n\nbash -n clean; node --check clean; tsc exit 0.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git add plugins/quote-selection build.mjs && git commit -q -F - <<'COMMITMSG'\nfeat(quote-selection): quote an assistant passage into the composer (#155)\n\nSelect text in an assistant message and a floating button offers to quote\nit; clicking APPENDS the passage to the composer draft as a markdown\nblockquote, so a half-typed message survives.\n\nShape: the button registers in conversation.input.dock (beside\ndurable-todos) and renders position:fixed from the selection's client\nrects. That is not cosmetic -- #148 established that conversation.chat.node,\nwhere a per-message button would naturally live, receives only the turnData\nhook plus seven owner props: no inputActions, no sessionId. A chat.node\nentry cannot write the composer at all. Living in the input zone and\npositioning against the viewport avoids building a cross-slot bridge.\n\nContainment is keyed on data-chat-flow-kind === \"assistant\", an attribute\nthe shipped scroll/paging code reads itself, rather than on any bubble or\nmarkdown class. So it survives the user-bubble takeover (which shadows only\nuser/steering keys) and needs no exclusion list: composer, modals, inputs\nand sidebar have no such ancestor and fail closed. Tool-call rows\ndeliberately do not arm; widening is one line in isAssistantFlowKind.\n\nFidelity: Range.toString() joins blocks with a single newline, which would\ncollapse a cross-paragraph quote into one line, so the client walks\nrange.cloneContents() into a small structural model and joins blank lines\nat block boundaries. A selection anchored in a <pre> is emitted fenced, so\nquoted code stays code.\n\nbuild.mjs gets a stanza, and that is the part worth reading twice: sync.sh\nbuilds plugins by running build.mjs, so a plugin with NO stanza there is\nnever rebuilt -- its lib/ keeps whatever bytes it was born with while its\nsrc/ moves on, silently, with no error anywhere. The worker hand-built the\nfirst bundles precisely because it could not add the stanza. Rebuilt here\nfrom the real stanza; the only difference from the hand-built output was\nthe esbuild css-text path comment, so the content was already correct.\n\nLogic lives in a pure module (quote.ts) because this repo has no jsdom\nharness; 43 tests across quote.test.ts and chrome.test.ts. Mutation:\ndropping the \"> \" prefix in toBlockquote turned 5 of 30 red, restored\nsha256-identical, green again.\n\ntsc clean; full suite 62 files / 1084 passed / 1 skipped (60/1041/1 before).\n\nNot visible until sync.sh runs: the plugin is registered in sync.sh's\npnpm_ins list and expected array, but installed nowhere yet.\nCOMMITMSG\ngit log -1 --format='%h %s'; git status --porcelain | head -6", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nfix(composer-approvals): declare liveHandlers above the badge gate (#150)\n\nThe composer attention badge never appeared for a pending approval or a\npending question. It was not a tone bug, a stale selector, or a missed\nsubscription -- every one of those was investigated and cleared. The\ncomponent was never rendering at all.\n\n`react.useRef` for liveHandlers sat BELOW `if (!badgeVisible(...)) return\nnull`. With nothing pending the render bailed after N hooks; the moment a\npending item arrived the render continued past the gate and ran N+1 hooks,\nwhich is React error #310 (\"rendered more hooks than during the previous\nrender\"). dsh-client-ui-renderer's SlotErrorBoundary caught the throw and\nLATCHED -- it sets {failed:true} and thereafter renders <div\ndata-slot-error> for the life of the mount. So one bad transition killed\nthe entry for the whole session, silently.\n\nThat explains every symptom at once: badge absent for questions too (the\ncrash is kind-agnostic); visible only after a page reload (remount clears\nthe latch, and the first render already had the item so the hook count\nstayed constant); typing did not help (a latched boundary does not\nre-run); no recent console error (the throw happens at the transition);\ncontext-meter missing from the same zone; and tool-render's inline card\nbar working fine throughout, since it is a different entry with its own\nuntripped boundary reading the SAME snapshot.pending through the SAME\nuseSession.\n\nDiagnosed from a live DOM probe by the owner, 2026-09-17, which is what\nfinally produced data-slot-error=\"conversation.input.left\" and then the\n#310 stack pointing at Indicator's useRef.\n\nThe file's own comment at the surfaces hook already stated the rule --\n\"Unconditional: hooks cannot sit below the badge-hidden early return\" --\nand this useRef went in 46 lines later anyway. A comment could not enforce\nit, so hooks.test.ts now does: it scans from the gate to the close of\nmakeIndicator and fails on any react.useX below it, plus pins this ref's\nposition specifically so a revert fails loudly rather than generically.\n\nMutation transcript: moving the declaration back below the gate turns 2 of\n3 tests red; the third (the gate-still-exists premise check) correctly\nstays green. Restored byte-identical, 3 green again.\n\nOnly the DECLARATION moved. The assignment stays below the gate where\njump/jumpableOf/handlesOf exist; the initial no-op value is never observed\nbecause the modal body reads .current only after openAttention(), which\ncannot run while the badge is hidden.\n\ntsc clean; full suite 60 files / 1041 passed / 1 skipped (1038 + 3 new).\n\nNOT staged: plugins/composer-menu/lib/client.js and\nplugins/user-bubble/lib/client.js carry comment-path churn only (React\n19.2.8 -> 19.3.0 store paths from the earlier lucide relink), code\nidentical; and sync.sh is the owner's own pin bump.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nrefactor: rename the React namespace alias so rules-of-hooks can see hooks (#158)\n\n#154 added eslint-plugin-react-hooks and then proved by probe that it was\nBLIND here: the rule recognises hook calls only through a namespace object\nliterally named `React` (hardcoded in getNodeWithoutReactNamespace /\nisReactFunction and in `callee.object.name === 'React'` checks). This repo\nwrote `react`, so rules-of-hooks matched nothing at all and #150's defect\nsailed through it.\n\nTHE GATE, verified in both directions rather than asserted. #150's exact\nmutation is the liveHandlers useRef moved below\n`if (!badgeVisible(tone, confirming)) return null;`:\n\n  before this commit:  npx eslint <file>  ->  exit 0, SILENT\n  after  this commit:  npx eslint <file>  ->  exit 1\n    955:24 error React Hook \"React.useRef\" is called conditionally\n\nRestored byte-identical, exit 0 again. I reproduced this myself after\nintegrating, not only from the worker's report.\n\nTHE TICKET'S SCOPE WAS WRONG AND THE WORKER SAID SO. My enumeration used\n`rg -l 'import \\* as react'` and found 6 files. Twelve more use the DEFAULT\nimport `import react from \"react\"`. One global JSX factory cannot serve both\ncasings: renaming only the namespace importers made tsc fail ~400 times with\n\"Cannot find name 'React'\". The rename had to extend to all of them, so\n\"tsc unchanged\" was unsatisfiable at the scope I wrote.\n\nTWO CONFIG FILES PINNED THE LOWERCASE FACTORY, and this is the part that\nwould have shipped a runtime crash. tsconfig.json's jsxFactory and\nbuild.mjs both named `react.createElement`. With build.mjs fixed and\ntsconfig not, the rebuild emitted bundles declaring `var React` while making\n21 `react.createElement` calls against a binding that no longer existed --\na guaranteed ReferenceError on render. The worker also established why:\nesbuild 0.28.2 resolves the JSX factory from tsconfig IN PREFERENCE TO the\nexplicit API flag.\n\nPurely mechanical: each file is byte-exactly HEAD plus the alias rename.\nPackage-name prose (the `react` external, lucide-react, @radix-ui/react-*)\nis untouched and was audited. plugins/quote-selection did not exist in the\nworker's worktree and was renamed here the same way: 4 candidate\noccurrences, 4 changed, alias-only.\n\neslint.config.mjs's blind-spot header is updated -- it is no longer true,\nand a stale warning is its own defect, which is precisely what #154 was\nabout.\n\ntsc exit 0. Suite 1093 passed / 1 skipped, unchanged. `npx eslint plugins`\nstill reports exactly the same 5 pre-existing findings in tool-render, same\nlines and messages: the rename adds none and removes none.\n\nBUNDLES ARE NOT REBUILT HERE, deliberately. The worker's rebuilt bundles\nembed its worktree paths, and committing them would bake that noise into\ntracked files. The committed lib/ output stays self-consistent (it was built\nfrom the pre-rename sources, which declared the lowercase binding), and\nsync.sh regenerates it through build.mjs with the corrected factory.\n\nKnown and left alone: plugins/shared/shims.d.ts carries a comment still\ndescribing the old convention. Not in this commit's allowlist; follow-up.\nCOMMITMSG\ngit log -1 --format='%h %s'", "cd /home/sid/repos/dotfiles-ai && git commit -q -F - <<'COMMITMSG'\nfeat(tool-render): run_code output is visible, and a refusal no longer reads as [object Object] (#152)\n\nPART A -- THE ABORT MESSAGE, AND THE TICKET'S PREMISE WAS WRONG.\nI specified patching dsh-tools' throw site, where the message is built. The\nworker found that could never have worked: `result.error.message` is ALREADY\nthe string \"[object Object]\" by the time dsh-tools sees it. The chain:\n\n  human rejects -> dsh-user-approval cancels with a plain object\n    {kind:\"user\", reason:\"approval-rejected\"}\n  -> run_code forwards it as the abort reason\n  -> dsh-code-runtime-worker-thread's onAbort does\n     `message: String(request.signal?.reason)`  <- the object dies HERE\n  -> dsh-tools interpolates the already-dead string.\n\nSo the serialiser belongs where the reason is still an object, plus a guard\nat the throw site so a structured message can never reach a template literal\nraw again. The twin at dsh-tools/lib/types/code-mode.js is deliberately NOT\npatched: the package exports map exposes no deep code-mode entry, so it is\ndead at runtime.\n\nHONEST FINDING ABOUT THE COMMENT: no layer carries the human's comment in\nthe abort reason -- the cancel cause has no comment field, and the approval\nwire does not either (tool-render steers comments as a separate message,\nwhich is how the model actually sees them). So the ticket's \"the comment\nmust survive\" criterion is not satisfiable at this seam. The serialiser\npreserves every extra string field on the reason, so a comment survives if\none is ever added upstream, and the refusal wording itself is now\nunmistakable:\n\n  code run failed (abort): user rejected the approval request (a human\n  refusal, not a crash -- do not retry the refused action)\n\ndistinguishable from: code run failed (timeout): compute budget exhausted.\n\nThe uncatchable-abort semantics are untouched. Both edits change only how\nthe reason READS -- a refusal must still kill the program.\n\nThe sync patch step (step_patch_dsh_run_code_abort) is new machinery for\npatching the installed dsh, so it is built to fail safely: idempotent\n(second run reports \"already applied\", files byte-identical), FAIL LOUD on\ndrift (a dsh upgrade that moves the code prints WARNING (#152) and\ncontinues rather than silently skipping -- a patch step that quietly stops\nworking is worse than none, because you believe you have the fix), and\n`node --check` after writing. Proven on fixture copies: apply, re-apply\nunchanged, and a simulated upgrade that trips the warning. The helpers live\nin one block in sync.sh and the test extracts that block verbatim, so the\nSHIPPED logic is tested rather than a copy.\n\nPART B -- THE OUTPUT ROW.\nCSS-only was rejected on verified structure, not guesswork: the shipped\nDisclosureRow renders `{open && children}`, so collapsed, the output node is\nabsent from the DOM entirely and no selector can reveal it.\n\nSo a narrow toolview shadow -- and the trap I warned about does not fire.\nToolCall renders the keyed toolview row and its children (.subCalls) as\nSIBLINGS, so shadowing the atomic run_code row leaves the nested subtool\nrendering untouched. That was the owner's explicit constraint and it is\npreserved by construction, not by reimplementation.\n\nLayout: collapsed row shows Code + description; the CODE is the collapsible\nbody; the OUTPUT is a permanent footer at the outer call's depth, visible\nwith zero clicks. Truncation mirrors today's rule exactly (upstream\n.ioSection is max-height 150px with internal scroll; the shadow caps at\n9.375rem the same way) -- no clamping, full text in the DOM. Empty output,\nincluding the \"(run_code completed with no output)\" sentinel, renders\nnothing rather than an empty labelled row.\n\nWhat the shadow reimplements, and will drift if upstream changes: summary\nderivation, body selection, and output flattening for exotic block types.\nRecorded in run-code.ts's header rather than left to be discovered.\n\nMutation: run-code-abort.test.ts fails 6 of 7 without the sync.sh block;\nthree further kill/revive passes on the real files (refusal branch, sentinel\nrule, description preference) each redden exactly their own test, restored\nbyte-identical, 17/17 green.\n\ntsc clean. Suite 1110 passed / 1 skipped (1093 before, +17). eslint reports\nthe same 5 pre-existing tool-render findings and no new ones. #158's rename\nis not regressed: zero lowercase react. uses in the touched files. dist was\nrebuilt from the main checkout, so no worktree paths are baked in.\n\nNOT VERIFIED HERE: the five live program shapes (log-only, return-only,\nboth, nested subtools, abort) need the GUI after a sync, and the DSH patch\nstep applies for real on the owner's next sync.\nCOMMITMSG\ngit log -1 --format='%h %s'"];
