# restart-pause — Debug settings panel (client plugin).
# The panel lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# the bundle; the host half (./lib) tracks which agents are mid-turn and
# registers the /restart-pause/* same-origin routes.
- insert:
    - id: restart-pause
      name: restart-pause
