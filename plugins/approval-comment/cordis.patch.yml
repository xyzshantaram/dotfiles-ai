# W8 — approval reject-with-comment (client plugin).
# The card seat replacement lives in the ./client bundle. The host row
# exists so the client-module registry mounts this package in the web
# profile and serves that bundle.
- insert:
    - id: approval-comment
      name: approval-comment
