# Composer overflow menu (client plugin).
# The menu lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# that bundle in the boot graph.
- insert:
    - id: composer-menu
      name: composer-menu
