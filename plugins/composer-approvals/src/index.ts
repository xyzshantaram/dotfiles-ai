/**
 * Host row of the pending-approval indicator. It exists only so the
 * client-module registry discovers the `dsh.client` entry; all behaviour
 * lives in the browser bundle.
 */
const name = "composer-approvals";
function apply() {}
export { apply, name };
