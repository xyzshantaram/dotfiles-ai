# log-viewer — dsh-web server log viewer panel (client plugin).
# The panel lives in the ./client bundle. The host row exists so the
# client-module registry mounts this package in the web profile and serves
# the bundle; the host half (./lib) registers the /log-viewer/lines
# same-origin route.
- insert:
    - id: log-viewer
      name: log-viewer
