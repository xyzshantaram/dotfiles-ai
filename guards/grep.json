{
  "commands": ["grep", "egrep", "fgrep", "zgrep"],
  "verdict": "deny",
  "readOnly": true,
  "reason": "grep is translated to rg and the rg form is what runs. rg is faster, searches directories by default, and skips hidden files and anything in .gitignore. Write rg yourself to avoid the substitution. When the translator cannot map a call it is denied instead, which happens when the command sits inside a wrapper such as sh -c, when an argument is an unquoted glob, or when an argument depends on a shell expansion.",
  "translate": "grep"
}
