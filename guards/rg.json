{
  "commands": ["rg"],
  "verdict": "allow",
  "readOnly": true,
  "rewrites": [
    {
      "drop": ["-r"],
      "value": true,
      "because": "bare -r is ambiguous: grep uses it for recursion, rg for output replacement. rg already recurses by default. For output substitution, write --replace explicitly."
    }
  ]
}
