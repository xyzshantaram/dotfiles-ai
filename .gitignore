node_modules/
server.env
# dsh personal bundle — built plugin bundles (dist/lib) are committed for distribution
# npm lockfiles inside plugin sub-packages (pnpm workspace; regenerated by npm install)
/plugins/*/package-lock.json
# Workspace paste storage (dsh-paste-to-path attachments)
/.dsh/
# sync.sh run log
/.sync-run.log
